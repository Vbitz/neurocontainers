name: template
version: 1.1.5
default_timeout: 120
tests:
- name: test datalad version
  description: Verify the container runtime smoke test for this tool.
  command: |
    set -eu
    output="$(datalad --version)"
    printf '%s\n' "${output}"
    printf '%s\n' "${output}" | grep -F datalad
